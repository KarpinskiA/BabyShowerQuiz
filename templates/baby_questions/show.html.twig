{% extends 'base.html.twig' %}

{% block title %}BabyQuestions{% endblock %}

{% block body %}
    <h1>BabyQuestions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ baby_question.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ baby_question.question }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_baby_questions_index') }}">back to list</a>

    <a href="{{ path('app_baby_questions_edit', {'id': baby_question.id}) }}">edit</a>

    {{ include('baby_questions/_delete_form.html.twig') }}
{% endblock %}
