{% extends 'base.html.twig' %}

{% block title %}ParentsQuestions{% endblock %}

{% block body %}
    <h1>ParentsQuestions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parents_question.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ parents_question.question }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parents_questions_index') }}">back to list</a>

    <a href="{{ path('app_parents_questions_edit', {'id': parents_question.id}) }}">edit</a>

    {{ include('parents_questions/_delete_form.html.twig') }}
{% endblock %}
